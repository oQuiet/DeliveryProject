Не стал реализовывывать сервис, интерфейс и репозиторий для одного select запроса в ручке /parcels-types

Решил создать LogRepository, чтобы был абстрактный класс который описывает работу с NoSQL базами данных

- [] Тесты
- [] Описание API через Swagger.

Решил не разделять задачи "создание посылки с расчетом стоимости и записью в бд" и "Запись расчета в лог" на разные воркеры

Может инициализировать все клиенты в config.py?

Не стал создавать request_id для логов, потому что проект учебный и запариваться по безопасности не стоит
Не стал реализовывать выбор нескольких типов посылок в /delivery_prices
Есть какое-то непонимание, страх и волнение использовать логирование везде где попало

Не стал реализовывать transactional outbox и идемпотентность запросов при использовании celery и RaabitMQ, потому что ушло бы еще куча времени, а списывать не хочу ибо все равно не понимаю.




6) /parcels/afb6029a-7b0c-4ab0-96c3-42b76d87894b/company - нет логирования
7) GET /delivery_prices?parcel_type_id=3 - нет логирования
8) /parcels/{parcel_id} - уровень логирования почему то WARNING при несуществующем UUID