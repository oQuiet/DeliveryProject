infrastructure/orm/database.py - для создания подлючения к БД с использованием пула и ORM подходом
*infrastructure/sql - можно будет создать для создания пула с использованием asyncpg.create_pool и чистым sql

Разворачивание базы данных в PostgreSQL 
    alembic upgrade head

Не стал реализовывывать сервис, интерфейс и репозиторий для одного select запроса в ручке /parcels-types

Решил реализовать CurrencyClient с двумя методами, update_currency работает как периодическая задача, чтобы всегда поддерживался актуальный курс доллара, а второй читает из кэша и в случае, если в кэше пусто, то вызывает update_currency.

Сам не знаю зачем создал сервис CalculateDeliveryPriceService, который выполняет сохранение цены доставки, когда можно было сделать в том же ParcelService.

Решил создать LogRepository, чтобы был абстрактный класс который описывает работу с NoSQL базами данных

Не понятно как называть коллекцию в MongoDB, класс реализующий методы работы с ней

ЗАчем мне mongo_client модуль

- [x] Зарегистрировать посылку.
- [x] Получить все типы посылок и id типов.
- [x] Получить список своих посылок
    - [x] Пагинация
    - [x] Фильтрация
    - [x] Вместо id типа посылки, возвращать ее название
- [x] Получить данные о посылке по ее id
- [x] Создать роут для привязки компании к посылке.
- [x] Если стоимость доставки еще не рассчитана - выводить “Не рассчитано”.
- [x] Добавить возможность запуска периодических задач вне расписания для нужд отладки.
    - [] Переписать эту задачу с использованием RabbitMQ и Celery
- [x] Добавить лог расчетов стоимости доставок и подсчет суммы стоимостей доставок используя MongoDB
- [x] Раз в 5 минут выставлять всем необработанным посылкам стоимости доставки в рублях.
- [x] Кешировать в redis курс доллара.
- [] Логирование.
- [] Обработка ошибок.
- [] Стандартизации ошибок и ответов.
- [] Чистота кода. 
- [] Dockerfile
- [] DockerCompose
- [] Тесты
- [] Добавить status_code для всех роутов
- [] Добавить retry для подключений к внешним системам
- [] Добавить индексы в базы данных
- [] стоит разобрать идемпотентность и transactional outbox связанный с не записью в MongoDB в случае ошибки
- [] У меня в разных частях приложения используются новые зависимости или используется одна, например redis_client, MongoLogRepository
- [] Решить вопрос с retry, expire и лимитов очереди для запроса курса доллара в celery


Решил не разделять задачи "создание посылки с расчетом стоимости и записью в бд" и "Запись расчета в лог" на разные воркеры (Можно было сначала воркер отрабатывает первую задачу, создает сообщение в RabbitMQ, воркер "записи в лог" берет сообщение и выполняет запись в MongoDB).


Может инициализировать все клиенты в config.py?